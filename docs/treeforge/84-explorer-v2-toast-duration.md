# Explorer V2 Toast Duration

Patch 095 verlängert Toast-/Info-Meldungen im Explorer V2.

## Änderung

Vorher:

```text
1800 ms / 2200 ms / 2500 ms / 3000 ms
```

Jetzt:

```text
4500 ms
```

## Grund

Die Meldungen verschwanden zu schnell, besonders bei Fehlern aus der Mutation API.