{# Core Node Template: unknown #}
<div id="{{ node.dom_id|default('tf-node-unknown') }}" class="{{ node.class|default('tf-node tf-node-unknown') }}" data-node-type="{{ node.original_type|default('unknown') }}">
  <strong>Unbekannter Node-Typ:</strong> {{ node.original_type|default('unknown') }}
</div>