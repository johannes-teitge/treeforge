{# Core Node Template: columns #}
<div id="{{ node.dom_id }}" class="{{ node.class }}" style="--tf-columns: {{ node.columns|default(2) }}; --tf-column-gap: {{ node.gap|default('1rem') }};">
  {{ node.children_html|default('')|raw }}
</div>